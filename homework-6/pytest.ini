[pytest]
testpaths = tests
# Coverage runs on every invocation so the number the gate reads is never a
# stale one from a different command.
addopts =
    --cov=pipeline
    --cov=orchestrator
    --cov-report=term-missing
    --cov-fail-under=80
